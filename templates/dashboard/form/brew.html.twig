<div class="border rounded-4 p-3 bg-dark text-white">
    <h6>Ajouter un producteur</h6>
    <article>
        <p>Ici ajoutez des producteurs pour ensuite les associers à des capsules.</p>
        <form action="{{ path('caps_create')}}" method="POST">
            <div class="form-group">
                <label for="name">Nom</label>
                <input type="text" class="form-control" name="name">
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary mt-3">Ajouter</button>
            </div>
        </form>
    </article>
    
    <article class="mt-3">
        <h6>Producteurs existants :</h6>
    </article>
    
</div>