{% extends 'base.html.twig' %}

{% form_theme form _self %}

{% block body %}
    {{ include('layouts/dashboardNav.html.twig') }}

    <section>
        <div class="row">
            <h2>Dashboard</h2>
        </div>
        <div class="row">
            <div class="col-6 p-2">
                <div class="border rounded-4 p-3">
                    <h6>Ajouter une capsule</h6>
                    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Cum eum similique culpa, deleniti, sit dolorum veritatis nemo, facilis ipsa optio quam. Nemo, aspernatur? Iure libero culpa nesciunt id, rem laudantium!</p>

                    {{ form_start(form) }}

                    {{ form_widget(form) }}
                    
                    {{ form_end(form) }}

                    {{ form_errors(form) }}
                </div>
            </div>

            <div class="col-6 p-2">
                {{ include('dashboard/form/brew.html.twig') }}
                {# <div class="border rounded-4 p-3 bg-dark text-white">
                  <h6>Ajouter un producteur</h6>
                </div> #}
            </div>
        </div>
        
    </section>

    {% block form_row %}
        <div class="form-group">
            {{ form_label(form) }}
            {{ form_widget(form) }}
        </div>
    {% endblock %}



{% endblock %}